package NetworkConnection;

public interface NetListener {
	/**
	 * Alex's
	 * 
	 *Interface which gets called when an Object is send, can registert to a server or a client
	 */

	public void update(Object object);

}
